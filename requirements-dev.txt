# Dépendances pour lancer la suite de tests (sans robot ni TFLite).
# Le SDK Reachy, tflite-runtime et zzlog sont mockés
# dans tests/conftest.py et n'ont pas besoin d'être installés.
numpy>=1.19.0,<2
opencv-python-headless>=4.5.0
Pillow>=8.0.0
pytest>=7.0

# Interface web (reachy_tictactoe/webapp) : testée avec TestClient, qui
# s'appuie sur httpx. uvicorn est importé par le point d'entrée
# (webapp/__main__.py) — dont le câblage est justement testé, puisque
# c'est lui qui plantait quand le SDK n'était pas encore prêt.
fastapi>=0.110
httpx>=0.27
uvicorn>=0.27
